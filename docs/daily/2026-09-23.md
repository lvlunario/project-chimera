# Daily engineering log — September 23, 2026

## 08:00 Asia/Manila — reopened-evidence JSON/HTML report candidate

Current phases: P0 approved by delegation; P1/P2 conditionally accepted for continued
development; personal walkthroughs unperformed; P3/P4 in progress; final acceptance open.
No repeated PM decision is requested this week. Read development head `46bef1c`, open draft
PR #1, open issues, program, requirements, architecture, roadmap, manual, quality plan,
approval guide, delegated decision record and September 22 daily evidence. PR #1 is open,
draft, unmerged and mergeable. No tracked root `AGENTS.md` exists. Reconstructed the exact
remote tree in a separate local workspace without overwriting earlier scratch work.

Implemented the bounded REPORT-001–006 candidate tracked by
[issue #15](https://github.com/lvlunario/project-chimera/issues/15): canonical schema-v1
`VerificationReport` from validated reopened evidence/bindings; exact evidence/binding/report
identities; COM-LINK-001/detail agreement; explicit unavailable/missing-evidence behavior;
optional synthetic source/plan/derived provenance; and deterministic self-contained HTML
with escaped evidence strings, no scripts/network assets, a restrictive content-security
policy and embedded canonical JSON. Input is bounded to 4 MiB.

Added `python -m examples.report_demo`: run synthetic fault/check/detail once, export/store/
reopen evidence, generate/reopen JSON and write/read temporary HTML without callback replay.
The example is not a dashboard, deployment or measured-data validation. Added focused
pass/fail/not-evaluated, conflict/tamper/duplicate/size, XSS/external-resource, optimized-mode
and durable integration controls; updated installed-wheel verification, manual, requirements,
architecture decision 0016, program and concise README. Initial focused verification:
`python -m unittest tests.test_reports -v` passed 8/8 tests in 0.254 s before subsequent
resource-bound coverage. Full corrected-candidate results and independent QA follow in this
log after execution; no unrun result is claimed here.

Engineering decision: canonical JSON remains authoritative and HTML may only project it.
The renderer never calculates a replacement verdict; contradictions stop report creation.
This makes presentation easier without creating a second source of engineering truth.

Known limits: API/demo only; no operator export endpoint/dashboard, signature, producer or
build authentication, complete raw-source bundle, PDF, measured-data validation or P4 gate
acceptance. Hashes are content identity only. No engineering blocker identified so far.

Next: complete full/optimized/clean-install and independent AI QA, fix any finding, then
publish only the verified candidate. After this slice, assemble the integrated October 17
workflow. October 3/17, November 7/28 and December 12 forecasts remain achievable; final
two weeks remain reserved for stabilization. No date or scope decision requested from Leo.

## 08:15 Asia/Manila — candidate verification and independent QA complete

Independent AI QA added nine permanent adversarial report tests and found that the initial
candidate did not fail closed on every malformed provenance or text boundary. The corrected
candidate now rejects non-strict saved fault plans, provenance/detail byte/count/minimum
disagreements, reversed manifest ranges, impossible replacement indices, contradictory
Boolean reasons, lone Unicode surrogates, C0/C1 control text and oversized reports. HTML
closing-tag injection remained escaped and script-free. The findings are tracked in
[issue #16](https://github.com/lvlunario/project-chimera/issues/16); no fictional human
review or acceptance is claimed.

Actual verification on the final local files:

- `python -m unittest discover -s tests -q`: 202/202 passed in 3.582 s;
- `python -O -m unittest discover -s tests -q`: 202/202 passed in 3.518 s;
- independent full runs: 202/202 passed normally in 3.531 s and under `-O` in 3.632 s;
- independent focused report runs: 17/17 passed normally and under `-O`;
- `scripts/verify_clean_install.sh`: isolated Python 3.12 wheel, installed report API and
  installed CLI checks passed;
- `python -m examples.report_demo`: failed COM-LINK-001, synthetic provenance, canonical
  JSON and 4,692-byte HTML were produced after callback counts remained one each; report
  generation replayed no task callback.

P4 remains in progress because operator workflow integration, broader presentation and its
gate evidence are not complete. P0 remains approved by delegation and P1/P2 conditionally
accepted for continued development; personal walkthroughs and final product acceptance
remain separate. No blocker. Candidate commit, hosted CI and PR/issue references will be
recorded in a later entry after they exist. Next task remains the integrated operator
entrypoint from workflow input through durable evidence, reporting and safe recovery.
