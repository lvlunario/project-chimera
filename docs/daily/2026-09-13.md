# 2026-09-13 — P1 packaging (Asia/Manila)

## 08:08 +08:00 — Clean-install increment

Current phase: P1 in progress. P0 concept/PM acceptance remains pending.
Implementation is AI-assisted; independent integration review is running.

### Goal and changes
- Added `pyproject.toml` for the `project-chimera` development distribution,
  Python 3.11+, Apache-2.0, zero runtime dependencies and `chimera-demo` entry point.
- Added `scripts/verify_clean_install.sh`: temporary source copy, wheel build, isolated
  virtual environment, offline/no-dependency wheel install and outside-checkout import.
- Added PACK-001–004 traceability, installation instructions and architecture decision 0003.
- Work item: [issue #5](https://github.com/lvlunario/project-chimera/issues/5).
  Integration: [draft PR #1](https://github.com/lvlunario/project-chimera/pull/1).

### Actual verification before commit
Environment: Linux, Python 3.12.14, pip 26.2.1, setuptools 84.0.0.
- `python -m unittest discover -s tests -v`: **31 passed**, 0.005 seconds.
- `scripts/verify_clean_install.sh`: built exactly one universal wheel; installed
  `project-chimera 0.1.0.dev0` with `--no-index --no-deps`; imported from the temporary
  venv outside the checkout; evidence API round trip passed; installed demo produced
  expected synthetic link failure and blocked report; cleanup completed.
- `git diff --check`: passed. No package was uploaded and no hardware/network used.

Independent AI integration QA repeated the 31-test suite (31 passed, 0.007 seconds),
shell syntax check and full clean install on Linux/Python 3.12.14. It inspected the
wheel contents (four Chimera modules plus expected distribution metadata/license/
entry point), confirmed cleanup and found no blocker. It noted the host build-backend
  prerequisite; an explicit Python/setuptools preflight and manual note were then added.
Final independent retest passed again: shell syntax, full clean-install workflow,
outside-checkout import and cleanup all verified with no blocker or major finding.
This is an actual AI review, not a human hire, release approval or PM acceptance.

Published implementation: [`f68bbe879599ca443be14e1281a24aac3dc59f3a`](https://github.com/lvlunario/project-chimera/commit/f68bbe879599ca443be14e1281a24aac3dc59f3a)
(`build(core): add verified installable package`). The connected GitHub plugin moved
the development branch by fast-forward; no force-push or merge occurred. A detached
worktree fetched from that exact published commit then passed the clean-install script
again on Linux/Python 3.12.14, confirming the remote files—not only local changes.

### Risks, next task and program status
Local evidence covers Python 3.12.14 only; Python 3.11 and clean CI runners remain
unverified. The wheel is unsigned and not guaranteed byte-reproducible. Building uses
the installed setuptools backend without isolation, while wheel installation itself
is index-free. This remains a development package, not a release.

Next task: add GitHub Actions CI for Python 3.11/3.12 to run the unit suite and clean
install check, then add strict user-supplied CLI workflow input. Do not close P1 until
all criteria pass. M0 Sep 19, M1 Oct 3 and prototype Dec 12 forecasts remain green;
no baseline change. Nov 29–Dec 12 remains reserved for stabilization.

Teaching point: source tests can conceal missing package files because Python imports
the checkout. This check changes directories and asserts the installed module path.
No new PM decision is required today. Existing D-001/D-002 scope decisions remain
due Sep 19; Monday Sep 14 concept review will present them.

## 15:05 +08:00 — Hosted CI matrix verified

Current phase: P1 in progress. P0 concept/PM acceptance remains pending.

### Changes and references
- Added `.github/workflows/test.yml` for pull requests and development/main pushes.
- Added Python 3.11/3.12 jobs; each runs the complete regression suite and isolated
  wheel-install verification. Jobs use read-only repository permission, no retained
  checkout credential, ten-minute timeout, branch concurrency and pinned Actions.
- Published [`35f3558d9b33b43be0790c7f9e124327abcef887`](https://github.com/lvlunario/project-chimera/commit/35f3558d9b33b43be0790c7f9e124327abcef887)
  (`ci(test): verify Python 3.11 and 3.12`) to
  [draft PR #1](https://github.com/lvlunario/project-chimera/pull/1), tracked by
  [issue #6](https://github.com/lvlunario/project-chimera/issues/6).
- Official action pins were resolved from upstream tag refs at review time:
  checkout v4 `11d5960a…`; setup-python v5 `a26af69b…`.

### Actual verification
Local pre-publication on Linux/Python 3.12.14: 31 tests passed in 0.005 seconds;
shell syntax and clean-install check passed; diff check passed.

[GitHub Actions run 34744334003](https://github.com/lvlunario/project-chimera/actions/runs/34744334003)
completed successfully on the published CI commit. Job/step inspection:
- Python 3.11 job `103689322142`: all steps succeeded; log shows 31 tests in
  0.006 seconds, installed `0.1.0.dev0` from the temporary Python 3.11
  `site-packages`, and `clean-install: passed`.
- Python 3.12 job `103689322250`: all steps succeeded; log shows 31 tests in
  0.007 seconds, installed `0.1.0.dev0` from the temporary Python 3.12
  `site-packages`, and `clean-install: passed`.

Independent AI CI/security QA reviewed trigger semantics, pins, permissions, shell
boundaries and documentation; it found no blocking defect. Local QA repeated the
31-test suite and clean install. Its final disposition for exact commit `35f3558d`
was PASS after independently confirming the run, exact head SHA, two successful jobs
and all step conclusions through the GitHub Actions API. The reviewer could not fetch
the public logs archive because GitHub returned 403, so it relied on the line-level
excerpts inspected through the connected GitHub access for counts and installed paths.
This is not human approval, release certification or PM acceptance.

### Risks, next task and teaching note
`ubuntu-latest` and downloaded setuptools/wheel remain mutable supply-chain inputs;
action code itself is commit-pinned. CI covers Linux only, not Windows/macOS. Hosted
success verifies this commit and does not automatically validate future commits.

Next task: strict CLI workflow input and exit codes, followed by evidence failure/
verdict categorization. P1 remains open until the CLI and full exit criteria pass.
M0 Sep 19, M1 Oct 3 and prototype Dec 12 remain green with no baseline change.
No new PM decision today; D-001/D-002 remain due Sep 19.

Teaching point: a CI matrix converts “Python 3.11 should work” into measured evidence.
Each interpreter starts fresh and proves both source behavior and installed-package
behavior. A green run applies to a specific commit, not every future version.

## 19:09 +08:00 — Declarative workflow CLI and daily close

Current phase: P1 in progress. Its implementation criteria now have evidence, but the
full gate review and Leo's acceptance remain pending; P0 concept acceptance also remains
pending. Work item: [issue #7](https://github.com/lvlunario/project-chimera/issues/7).

### Changes and references
- Added `chimera run WORKFLOW --evidence OUTPUT` with schema-versioned UTF-8 JSON,
  strict fields, bounded input/tasks/dependencies and only synthetic `emit`/`fail`
  operations. No workflow field can import Python, run a shell or contact a service.
- Added distinct exit codes for all-success (0), completed unsuccessful run (1),
  invalid input/graph/usage (2) and evidence-output failure (3). Completed failures
  retain evidence; an existing output is never replaced.
- Added an executable example, installed-wheel CLI proof, CLI-001–005 traceability,
  architecture decision 0004, manual/operator instructions and teaching exercise.
- Published [`2456353507cd4ed7bc176d3bfb2af18f402e76da`](https://github.com/lvlunario/project-chimera/commit/2456353507cd4ed7bc176d3bfb2af18f402e76da)
  (`feat(core): add strict workflow CLI`) and the follow-up executable-mode repair
  [`1ea07c7ee12336d8b116e864f9cd9f8065e6b781`](https://github.com/lvlunario/project-chimera/commit/1ea07c7ee12336d8b116e864f9cd9f8065e6b781)
  (`fix(core): restore install verifier executable mode`) to
  [draft PR #1](https://github.com/lvlunario/project-chimera/pull/1). Both branch
  updates were fast-forwards; no merge or force-push occurred.

### Actual verification and QA
Local Linux/Python 3.12.14 before publication: **49 tests passed** in 0.092 seconds;
shell syntax, `git diff --check`, direct CLI failure/evidence demo and full isolated
wheel install passed. The installed `chimera` command wrote and reopened four-task
schema-v1 evidence and returned expected exit 1.

Independent AI QA added eight negative-path tests and repeated the full suite and
clean install. It attacked nested duplicate keys, 101-level values, task/dependency
limits, a 5,000-digit integer, invalid graphs, an attempted `__import__` operation,
existing output and an unwritable destination. It found one minor error-normalization
defect for huge integers; implementation was corrected and retested. Final disposition:
PASS with 49/49 tests and no code-execution, overwrite or exit-code defect. This is an
actual AI review, not human approval, a hire, certification or PM acceptance.

Initial [hosted run 34753583650](https://github.com/lvlunario/project-chimera/actions/runs/34753583650)
truthfully failed after both Python jobs passed all 49 tests: publication had changed
the install script from executable to regular-file mode, so the next step exited 126.
The mode-only fix above restored it. Replacement
[run 34753672209](https://github.com/lvlunario/project-chimera/actions/runs/34753672209)
passed both Python 3.11 and 3.12 jobs; inspected steps/logs show 49 tests and the full
installed-wheel CLI/evidence check passed on each interpreter.

### Risks, next task and cumulative day status
An I/O failure during the current direct evidence write can leave a partial file, and
output writability after a race may be learned only after synthetic tasks run. This is
acceptable only for today's side-effect-free allowlist; atomic durable storage belongs
to P2 before real adapters. Limits reduce accidental resource use but are not hostile-
input sandboxing. The failed CI run is retained as process evidence, not hidden.

Today delivered an installable zero-dependency wheel, verified Python 3.11/3.12 CI,
and the strict operator CLI. The regression suite grew from 31 to 49 tests. P1 remains
open for execution-error versus requirement-verdict categorization and the full gate
review. Next task: prepare Monday's communications-link concept review, then implement
that outcome model without conflating task execution success with requirement pass.

Forecast remains green: M0 September 19, M1 October 3 and prototype December 12.
No new PM decision is required tonight. D-001/D-002 remain due September 19, with the
recommendation to retain the communications-link vertical and keep AI/live hardware
outside the required prototype. Teaching point: a failed verification is still a valid
test record (exit 1); malformed procedure input is not a run at all (exit 2).
